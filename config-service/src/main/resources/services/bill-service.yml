spring:
  jpa:
    database: POSTGRESQL
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
    hibernate:
      ddl-auto: create
    properties:
  datasource:
    platform: postgres
    url: jdbc:mysql://localhost:3306/bill_service_databaseuseSSL=false

    username: kali
    password: kali
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  servlet:
    context-path: /bills
  port: 8082
